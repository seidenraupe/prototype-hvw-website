/* HVW Prototype — museumschaffen.ch visual language, modern UX */

:root {
  --color-bg: #ffffff;
  --color-text: #1a1a1a;
  --color-muted: #555555;
  --color-line: #1a1a1a;
  --color-accent: #5b9fd4;
  --color-accent-dark: #3d7aad;
  --color-cta: #1a1a1a;
  --font-base: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
  --font-size-base: 18px;
  --line-height: 1.6;
  --max-width: 1280px;
  --nav-height: 64px;
  --space-xs: 0.5rem;
  --space-sm: 1rem;
  --space-md: 1.5rem;
  --space-lg: 2.5rem;
  --space-xl: 4rem;
}

*, *::before, *::after { box-sizing: border-box; }

html {
  font-size: var(--font-size-base);
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: var(--font-base);
  font-size: 1rem;
  line-height: var(--line-height);
  color: var(--color-text);
  background: var(--color-bg);
  -webkit-font-smoothing: antialiased;
}

img { max-width: 100%; height: auto; display: block; }

a {
  color: inherit;
  text-decoration-thickness: 1px;
  text-underline-offset: 3px;
}

a:hover { opacity: 0.75; }

h1, h2, h3, h4 {
  margin: 0 0 var(--space-sm);
  font-weight: 600;
  line-height: 1.2;
}

h1 { font-size: clamp(1.75rem, 4vw, 2.75rem); }
h2 { font-size: clamp(1.25rem, 2.5vw, 1.75rem); text-transform: uppercase; letter-spacing: 0.04em; }
h3 { font-size: 1.125rem; }

p { margin: 0 0 var(--space-sm); }

ul { margin: 0; padding: 0; list-style: none; }

/* ── Layout helpers ── */

.container {
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 var(--space-md);
}

.grid-line { border-top: 1px solid var(--color-line); }

.section-label {
  display: block;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: var(--space-sm);
}

.section { padding: var(--space-lg) 0; }

/* ── Ticker ── */

.ticker {
  border-bottom: 1px solid var(--color-line);
  background: var(--color-bg);
  overflow: hidden;
  white-space: nowrap;
}

.ticker__inner {
  display: inline-block;
  padding: var(--space-xs) var(--space-md);
  font-size: 0.875rem;
  font-weight: 500;
  animation: ticker 30s linear infinite;
}

@keyframes ticker {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

/* ── Header / Nav ── */

.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: var(--color-bg);
  border-bottom: 1px solid var(--color-line);
}

.site-header__inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: var(--nav-height);
  gap: var(--space-md);
}

.logo {
  display: flex;
  align-items: center;
  text-decoration: none;
  flex-shrink: 0;
  line-height: 0;
}

.logo:hover { opacity: 1; }

.logo img {
  display: block;
  height: 40px;
  width: auto;
}

.nav-desktop {
  display: flex;
  align-items: center;
  gap: var(--space-md);
}

.nav-desktop a {
  text-decoration: none;
  font-size: 0.9rem;
  font-weight: 500;
  white-space: nowrap;
}

.nav-desktop a.is-active {
  text-decoration: underline;
  text-underline-offset: 4px;
}

.btn-cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.6rem 1.25rem;
  background: var(--color-cta);
  color: #fff !important;
  text-decoration: none !important;
  font-weight: 600;
  font-size: 0.9rem;
  border: none;
  cursor: pointer;
  min-height: 48px;
  transition: background 0.15s;
}

.btn-cta:hover { background: #333; opacity: 1; }

.btn-cta--accent {
  background: var(--color-accent-dark);
}

.btn-cta--accent:hover { background: var(--color-accent); }

.btn-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.6rem 1.25rem;
  border: 1px solid var(--color-line);
  background: transparent;
  color: var(--color-text);
  text-decoration: none !important;
  font-weight: 500;
  font-size: 0.9rem;
  min-height: 48px;
}

.btn-secondary:hover { background: #f5f5f5; opacity: 1; }

.nav-toggle {
  display: none;
  background: none;
  border: 1px solid var(--color-line);
  padding: 0.5rem 0.75rem;
  font-size: 0.875rem;
  cursor: pointer;
  min-height: 44px;
  min-width: 44px;
}

.nav-mobile {
  display: none;
  border-top: 1px solid var(--color-line);
  padding: var(--space-sm) var(--space-md) var(--space-md);
}

.nav-mobile.is-open { display: block; }

.nav-mobile a {
  display: block;
  padding: 0.75rem 0;
  text-decoration: none;
  font-weight: 500;
  border-bottom: 1px solid #eee;
}

.nav-mobile .btn-cta {
  width: 100%;
  margin-top: var(--space-sm);
}

@media (max-width: 900px) {
  .nav-desktop { display: none; }
  .nav-toggle { display: block; }
}

/* ── Hero ── */

.hero {
  display: grid;
  grid-template-columns: 1fr 1fr;
  border-bottom: 1px solid var(--color-line);
}

.hero__text {
  padding: var(--space-xl) var(--space-lg);
  display: flex;
  flex-direction: column;
  justify-content: center;
  border-right: 1px solid var(--color-line);
}

.hero__statement {
  font-size: clamp(1.5rem, 3vw, 2.25rem);
  font-weight: 600;
  line-height: 1.25;
  margin-bottom: var(--space-md);
  text-transform: none;
}

.hero__lead {
  color: var(--color-muted);
  font-size: 1.05rem;
  margin-bottom: var(--space-lg);
  max-width: 42ch;
}

.hero__actions {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
}

.hero__visual {
  position: relative;
  min-height: 360px;
  background: var(--color-accent);
  display: flex;
  align-items: flex-end;
  overflow: hidden;
}

.hero__visual-inner {
  width: 100%;
  padding: var(--space-lg);
  background: linear-gradient(transparent 20%, rgba(0,0,0,0.55));
  color: #fff;
}

.hero__visual-placeholder {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(135deg, var(--color-accent) 0%, #7eb8e0 50%, #3d7aad 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: rgba(255,255,255,0.5);
}

@media (max-width: 768px) {
  .hero { grid-template-columns: 1fr; }
  .hero__text {
    border-right: none;
    border-bottom: 1px solid var(--color-line);
    padding: var(--space-lg) var(--space-md);
  }
  .hero__visual { min-height: 240px; }
}

/* ── Event preview cards ── */

.events-preview {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0;
  border: 1px solid var(--color-line);
}

.event-card {
  padding: var(--space-md);
  border-right: 1px solid var(--color-line);
  text-decoration: none;
  color: inherit;
  transition: background 0.15s;
}

.event-card:last-child { border-right: none; }
.event-card:hover { background: #f8f8f8; opacity: 1; }

.event-card__date {
  font-size: 0.8rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: var(--space-xs);
}

.event-card__title {
  font-weight: 600;
  margin-bottom: var(--space-xs);
}

.event-card__meta {
  font-size: 0.85rem;
  color: var(--color-muted);
}

.events-preview__more {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-md);
  border-top: 1px solid var(--color-line);
  text-align: center;
}

@media (max-width: 768px) {
  .events-preview { grid-template-columns: 1fr; }
  .event-card { border-right: none; border-bottom: 1px solid var(--color-line); }
  .event-card:last-child { border-bottom: none; }
}

/* ── Museums grid ── */

.museums-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  border: 1px solid var(--color-line);
}

.museum-card {
  border-right: 1px solid var(--color-line);
  text-decoration: none;
  color: inherit;
}

.museum-card:last-child { border-right: none; }
.museum-card:hover { opacity: 0.85; }

.museum-card__img {
  aspect-ratio: 4/3;
  background: #e8e8e8;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-muted);
  border-bottom: 1px solid var(--color-line);
}

.museum-card__body { padding: var(--space-md); }

.museum-card__title {
  font-weight: 600;
  text-transform: uppercase;
  font-size: 0.9rem;
  letter-spacing: 0.04em;
  margin-bottom: var(--space-xs);
}

.museum-card__desc {
  font-size: 0.9rem;
  color: var(--color-muted);
}

@media (max-width: 768px) {
  .museums-grid { grid-template-columns: 1fr; }
  .museum-card { border-right: none; border-bottom: 1px solid var(--color-line); }
  .museum-card:last-child { border-bottom: none; }
}

/* ── Quote section ── */

.quote-feature {
  display: grid;
  grid-template-columns: 200px 1fr;
  gap: var(--space-lg);
  align-items: start;
  border: 1px solid var(--color-line);
  padding: var(--space-lg);
}

.quote-feature__avatar {
  width: 200px;
  height: 200px;
  background: #e0e0e0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.7rem;
  text-transform: uppercase;
  color: var(--color-muted);
  letter-spacing: 0.06em;
}

.quote-feature__text {
  font-size: 1.15rem;
  font-style: italic;
  line-height: 1.5;
  margin-bottom: var(--space-sm);
}

.quote-feature__author {
  font-size: 0.9rem;
  font-style: normal;
  font-weight: 600;
}

.quote-feature__role {
  font-size: 0.85rem;
  color: var(--color-muted);
}

.quote-chips {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xs);
  margin-top: var(--space-md);
}

.quote-chip {
  padding: 0.4rem 0.8rem;
  border: 1px solid var(--color-line);
  font-size: 0.8rem;
  text-decoration: none;
  font-weight: 500;
}

.quote-chip:hover,
.quote-chip.is-active {
  background: var(--color-text);
  color: #fff;
  opacity: 1;
}

@media (max-width: 600px) {
  .quote-feature { grid-template-columns: 1fr; }
  .quote-feature__avatar { width: 100%; height: 160px; }
}

/* ── Membership cards ── */

.membership-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  border: 1px solid var(--color-line);
}

.membership-card {
  padding: var(--space-md);
  border-right: 1px solid var(--color-line);
  display: flex;
  flex-direction: column;
}

.membership-card:last-child { border-right: none; }

.membership-card__type {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: var(--space-xs);
}

.membership-card__price {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: var(--space-sm);
}

.membership-card__benefits {
  font-size: 0.85rem;
  color: var(--color-muted);
  flex: 1;
  margin-bottom: var(--space-md);
}

.membership-card__benefits li {
  padding: 0.2rem 0;
  padding-left: 1rem;
  position: relative;
}

.membership-card__benefits li::before {
  content: "✓";
  position: absolute;
  left: 0;
  font-weight: 600;
}

@media (max-width: 900px) {
  .membership-grid { grid-template-columns: repeat(2, 1fr); }
  .membership-card:nth-child(2) { border-right: none; }
  .membership-card { border-bottom: 1px solid var(--color-line); }
}

@media (max-width: 500px) {
  .membership-grid { grid-template-columns: 1fr; }
  .membership-card { border-right: none; }
}

/* ── Newsletter ── */

.newsletter-form {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
  max-width: 560px;
}

.newsletter-form input {
  flex: 1;
  min-width: 200px;
  padding: 0.75rem 1rem;
  font-size: 1rem;
  border: 1px solid var(--color-line);
  font-family: inherit;
}

/* ── Page header ── */

.page-header {
  padding: var(--space-lg) 0 var(--space-md);
  border-bottom: 1px solid var(--color-line);
}

.page-header h1 {
  text-transform: uppercase;
  letter-spacing: 0.04em;
  font-size: clamp(1.5rem, 3vw, 2rem);
}

.page-header__lead {
  color: var(--color-muted);
  max-width: 60ch;
  margin-top: var(--space-sm);
}

/* ── Eventfrog embed ── */

.eventfrog-wrapper {
  border: 1px solid var(--color-line);
  min-height: 700px;
  position: relative;
}

.eventfrog-wrapper iframe {
  display: block;
  width: 100%;
  min-height: 700px;
  border: none;
}

.eventfrog-note {
  padding: var(--space-sm) var(--space-md);
  background: #f5f5f5;
  border: 1px solid var(--color-line);
  border-bottom: none;
  font-size: 0.85rem;
  color: var(--color-muted);
}

/* ── Eventfrog Public API — Startseiten-Vorschau (harte Begrenzung auf 3) ──
   Die 3 nächsten Veranstaltungen werden per GitHub Action periodisch über
   die Eventfrog Public API geladen (siehe scripts/fetch-eventfrog-events.mjs)
   und als data/home-events.json abgelegt. js/main.js rendert daraus bis zu
   3 .event-card-Elemente in das bestehende .events-preview-Grid. */

.events-preview__status {
  grid-column: 1 / -1;
  padding: var(--space-md);
  text-align: center;
  color: var(--color-muted);
}

/* ── Quote cards (zitate page) ── */

.quotes-list {
  display: flex;
  flex-direction: column;
  gap: 0;
  border: 1px solid var(--color-line);
}

.quote-item {
  padding: var(--space-lg);
  border-bottom: 1px solid var(--color-line);
}

.quote-item:last-child { border-bottom: none; }

.quote-item__question {
  font-size: 0.8rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-muted);
  margin-bottom: var(--space-sm);
}

.quote-item__answer {
  font-size: 1.1rem;
  line-height: 1.55;
  margin-bottom: var(--space-sm);
}

.quote-item__person {
  font-weight: 600;
  font-size: 0.9rem;
}

.quote-item__role {
  font-size: 0.85rem;
  color: var(--color-muted);
}

/* ── Webling info box ── */

.info-box {
  border: 1px solid var(--color-line);
  padding: var(--space-lg);
  background: #fafafa;
  margin-bottom: var(--space-lg);
}

.info-box h3 {
  text-transform: uppercase;
  font-size: 0.85rem;
  letter-spacing: 0.06em;
}

.info-box p:last-child { margin-bottom: 0; }

/* ── Webling-Anmeldeformular (iframe-Embed) ──
   Das Formular wird eingebettet statt in einem neuen Tab geöffnet, damit
   die Besucherin/der Besucher nach dem Absenden weiterhin auf der
   HVW-Website bleibt (Webling selbst bietet keine Weiterleitungs-Option
   nach dem Abschicken des Formulars). */

#anmeldeformular {
  /* Damit die Überschrift beim Ankersprung nicht unter dem sticky Header
     verschwindet. */
  scroll-margin-top: calc(var(--nav-height) + var(--space-md));
}

.webling-embed {
  border: 1px solid var(--color-line);
  min-height: 900px;
}

.webling-embed iframe {
  display: block;
  width: 100%;
  min-height: 900px;
  border: none;
}

.webling-embed__back {
  display: flex;
  justify-content: center;
  margin-top: var(--space-md);
}

/* ── Stats row ── */

.stats-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  border: 1px solid var(--color-line);
}

.stat-item {
  padding: var(--space-md);
  border-right: 1px solid var(--color-line);
  text-align: center;
}

.stat-item:last-child { border-right: none; }

.stat-item__number {
  font-size: 2rem;
  font-weight: 600;
  line-height: 1;
  margin-bottom: var(--space-xs);
}

.stat-item__label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-muted);
}

@media (max-width: 600px) {
  .stats-row { grid-template-columns: repeat(2, 1fr); }
  .stat-item:nth-child(2) { border-right: none; }
  .stat-item { border-bottom: 1px solid var(--color-line); }
}

/* ── Footer ── */

.site-footer {
  border-top: 1px solid var(--color-line);
  padding: var(--space-lg) 0;
  margin-top: var(--space-xl);
}

.site-footer__grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: var(--space-lg);
}

.site-footer h4 {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: var(--space-sm);
}

.site-footer a {
  display: block;
  text-decoration: none;
  font-size: 0.9rem;
  padding: 0.2rem 0;
}

.site-footer__bottom {
  margin-top: var(--space-lg);
  padding-top: var(--space-md);
  border-top: 1px solid var(--color-line);
  font-size: 0.85rem;
  color: var(--color-muted);
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: var(--space-sm);
}

@media (max-width: 768px) {
  .site-footer__grid { grid-template-columns: 1fr; }
}

/* ── Prototype badge ── */

.prototype-badge {
  position: fixed;
  bottom: var(--space-sm);
  right: var(--space-sm);
  background: var(--color-text);
  color: #fff;
  font-size: 0.7rem;
  padding: 0.35rem 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  z-index: 200;
  pointer-events: none;
}

/* ── Two-column content ── */

.two-col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-lg);
}

@media (max-width: 768px) {
  .two-col { grid-template-columns: 1fr; }
}

/* ── Publikationen list ── */

.pub-list { border: 1px solid var(--color-line); }

.pub-item {
  display: grid;
  grid-template-columns: 120px 1fr auto;
  gap: var(--space-md);
  padding: var(--space-md);
  border-bottom: 1px solid var(--color-line);
  align-items: center;
}

.pub-item:last-child { border-bottom: none; }

.pub-item__year {
  font-weight: 600;
  font-size: 0.9rem;
}

.pub-item__title { font-weight: 500; }

.pub-item__link {
  font-size: 0.85rem;
  white-space: nowrap;
}

@media (max-width: 600px) {
  .pub-item { grid-template-columns: 1fr; }
}

/* ── Sammlung portfolio ── */

.collection-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  border: 1px solid var(--color-line);
}

.collection-card {
  border-right: 1px solid var(--color-line);
  border-bottom: 1px solid var(--color-line);
}

.collection-card:nth-child(3n) { border-right: none; }
.collection-card:nth-last-child(-n+3) { border-bottom: none; }

.collection-card__img {
  aspect-ratio: 4/3;
  background: #e8e8e8;
  border-bottom: 1px solid var(--color-line);
  overflow: hidden;
}

.collection-card__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.collection-card__img--placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-muted);
}

.collection-card__body { padding: var(--space-md); }

.collection-card__title {
  font-weight: 600;
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin: 0 0 var(--space-xs);
}

.collection-card__desc {
  font-size: 0.9rem;
  color: var(--color-muted);
  margin: 0;
}

@media (max-width: 900px) {
  .collection-grid { grid-template-columns: repeat(2, 1fr); }
  .collection-card { border-right: 1px solid var(--color-line); }
  .collection-card:nth-child(3n) { border-right: 1px solid var(--color-line); }
  .collection-card:nth-child(2n) { border-right: none; }
  .collection-card:nth-last-child(-n+3) { border-bottom: 1px solid var(--color-line); }
  .collection-card:nth-last-child(-n+2) { border-bottom: none; }
}

@media (max-width: 600px) {
  .collection-grid { grid-template-columns: 1fr; }
  .collection-card,
  .collection-card:nth-child(2n),
  .collection-card:nth-child(3n) {
    border-right: none;
    border-bottom: 1px solid var(--color-line);
  }
  .collection-card:last-child { border-bottom: none; }
}

/* ── Mobile sticky bar ── */

.mobile-sticky {
  display: none;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--color-bg);
  border-top: 1px solid var(--color-line);
  z-index: 90;
  padding: var(--space-xs);
  gap: var(--space-xs);
}

.mobile-sticky a {
  flex: 1;
  text-align: center;
  padding: 0.75rem;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.85rem;
  min-height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.mobile-sticky a:first-child {
  background: var(--color-cta);
  color: #fff;
}

@media (max-width: 900px) {
  .mobile-sticky { display: flex; }
  body { padding-bottom: 60px; }
}
